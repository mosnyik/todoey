import 'package:flutter/material.dart';

const Color kAppBackGroundColor = Color(0xFF635608);
const Color kWhite = Colors.white;
const Color kAddTaskScreenBackground = Color.fromRGBO(117, 117, 117, 1);
const kTaskScreenDisplayPadding =
    EdgeInsets.only(left: 30, top: 60.0, right: 30, bottom: 30);
    // 9b870cß
